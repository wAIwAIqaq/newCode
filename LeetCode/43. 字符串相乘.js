/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var multiply = function (num1, num2) {
  let i = num1.length - 1;
  while (i >= 0) {
    let j = num2.length - 1;
    let curMult = "";
    while (j >= 0) {}
    i--;
  }
};
console.log(multiply("123", "456"));
